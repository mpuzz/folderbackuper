<Window x:Class="FolderBackup.Client.ControlView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FolderBackup.Client"
        mc:Ignorable="d"
        Title="ControlView" Height="467" Width="919">
    <Grid>

        <TabControl x:Name="tabControl" Margin="0,0,6,41">
            <TabItem Header="Verisons">
                <Grid Background="#FFFFFF" Margin="0,0,0,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="252*"/>
                        <ColumnDefinition Width="647*"/>
                    </Grid.ColumnDefinitions>
                    <TreeView x:Name="versionView" Margin="10,20,20,20" Grid.Column="1">

                    </TreeView>
                    <ListBox x:Name="versionBox" Margin="20,20,10,20"/>
                </Grid>
            </TabItem>
            <TabItem Header="Settings">
                <Grid Background="#FFFFFFFF">

                </Grid>
            </TabItem>
        </TabControl>
        <Label x:Name="errorBox" Content="" Background="White" FontSize="12" Foreground="#FFCC0000" FontFamily="Calibri" BorderThickness="1" Margin="0,400,6,5"/>

    </Grid>
</Window>
